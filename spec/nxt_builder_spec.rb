require 'spec_helper'

describe NxtBuilder do
  it 'has a version number' do
    expect(NxtBuilder::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
